// script.js (bisa untuk interaksi global jika ada)
// Contoh: Inisialisasi tooltips Bootstrap jika digunakan
// document.addEventListener('DOMContentLoaded', function () {
//     var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
//     var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
//         return new bootstrap.Tooltip(tooltipTriggerEl)
//     })
// });

// Kode untuk blur options di foto_profil.html sudah ada di dalam template itu sendiri.
// Jika ada fungsi JS yang lebih kompleks atau digunakan di banyak halaman, letakkan di sini.
console.log("Global script.js loaded.");
